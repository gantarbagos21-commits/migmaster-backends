{
  "name": "migmaster-cloudflare-v5",
  "version": "5.0.2-cloudflare-final-v2-anti-stall-vote",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config ./wrangler.json",
    "dev": "wrangler dev --config ./wrangler.json",
    "check": "wrangler deploy --dry-run --config ./wrangler.json"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
