{
  "name": "migmaster-cloudflare-backend",
  "version": "1.0.0",
  "private": true,
  "description": "MigMaster WebSocket bridge for Cloudflare Workers + Durable Objects",
  "type": "module",
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy" },
  "devDependencies": { "wrangler": "^4.68.0" }
}
